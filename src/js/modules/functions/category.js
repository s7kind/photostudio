module.exports = (wrap) => {


    /*const categoryArr = wrap.querySelectorAll('.category-wrap__item');
    let img, imgArr;

    for (let i = 0; i < categoryArr.length; i++) {
        categoryArr[i].addEventListener('mouseover', function (event) {
            removeActive(categoryArr);
            event.currentTarget.classList.add('active');
            img = categoryArr[i].querySelector('img');
            img.style.width = `100%`;
        });
    }

    function removeActive(arr) {
        for (let i = 0; i < arr.length; i++) {
            arr[i].classList.remove('active');
            imgArr = arr[i].querySelector('img');
            imgArr.style.width = `33%`;
        }
    }*/


};